# AnimusFlow — Public Theme Assets

Binários públicos (vídeos, imagens, PDFs) descarregados automaticamente
pela instalação de temas via prompt no AnimusFlow.

Estrutura espelha `public/`: `videos/`, `images/`, `docs/`.
